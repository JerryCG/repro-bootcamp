---
title: "Do Governments Censor the Internet More During Armed Conflict?"
author: "<your name>"
date: "`r Sys.Date()`"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, message = FALSE, warning = FALSE)
library(here)
library(tidyverse)
library(modelsummary)

# Results exist only once code/05_analysis.R has been written and run.
results_file <- here("output", "tables", "main_results.rds")
have_results <- file.exists(results_file)
```

## Introduction

Governments restrict access to information when they feel threatened. Armed conflict is a prominent threat: it raises the stakes of controlling what citizens see and share, and it gives rulers a security rationale for doing so. This paper asks whether governments censor the internet more when armed conflict takes place on their territory, and whether this effect is stronger in autocracies, where rulers face fewer institutional and legal constraints on censorship.

## Data

We combine three sources. Internet censorship, regime type, and country identifiers come from the V-Dem Country-Year dataset (v16); armed conflict comes from the UCDP/PRIO Armed Conflict Dataset (version 26.1); GDP per capita and internet use come from the World Bank's World Development Indicators. The unit of analysis is the country-year.

```{r load-results, include=FALSE, eval=have_results}
results <- readRDS(results_file)
```

The confirmatory estimation sample contains `r results$analysis_numbers$n` country-years from `r results$analysis_numbers$countries` countries, covering `r results$analysis_numbers$first_year` through `r results$analysis_numbers$last_year`.

## Research Design

We estimate a two-way fixed-effects model of censorship on conflict, its interaction with autocracy, and two controls, with country and year fixed effects and standard errors clustered by country. The specification, sample, and inference criteria are fixed in the pre-analysis plan (`pap/pre_analysis_plan.md`), which was committed before the data were downloaded.

## Results (confirmatory)

The estimated effect of conflict is `r results$coefficient_table |> filter(term == "conflict") |> pull(estimate)` (clustered standard error = `r results$coefficient_table |> filter(term == "conflict") |> pull(std_error)`).

```{r confirmatory-model, eval=have_results}
modelsummary(results$model, stars = FALSE)
```

```{r descriptive-figure, eval=have_results, fig.cap="Mean government internet filtering by armed conflict status"}
knitr::include_graphics(here("output", "figures", "descriptive_censorship_by_conflict.png"))
```

```{r todo-guard, eval=!have_results, results='asis'}
cat("*Results are not available yet: complete the TODOs and run the analysis script.*")
```

## Exploratory analyses

<!-- Everything that is not in the pre-analysis plan goes here, clearly labeled as exploratory. -->

## Reproducibility statement

<!-- Fill in at the end: how to re-run everything (Rscript run_all.R), software versions (sessionInfo()). -->
